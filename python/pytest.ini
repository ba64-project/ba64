[pytest]
testpaths = tests
addopts = -q -m "not slow"
markers =
    slow: heavy perf/IT tests (large blobs, big CLI streams); excluded from the
        default + mutation runs, exercised explicitly via `pytest -m slow`.
